import 'package:flutter/material.dart';

class CollectionPageWidget extends StatelessWidget {
  const CollectionPageWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('收藏'),
    );
  }
}